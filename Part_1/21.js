/* Array */

let arr=[10,20,30,40];
console.log(arr.slice(0,3));  //last postion will be exclude

console.log(typeof arr);

let obj={};
console.log(typeof obj); // type check 

let fname=["Pritesh","Bhatiya",10,20,30];

console.log(Array.isArray(arr));  //true   check isArray or not
console.log(Array.isArray(obj));  //false 
console.log(Array.isArray(fname));


/* In JavaScript, the isArray method is used to determine whether a 
given value is an array or not. It is a built-in method provided by the 
Array object, and it returns true if the specified value is an array, and 
false otherwise.*/

